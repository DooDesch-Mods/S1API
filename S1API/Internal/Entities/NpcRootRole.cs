namespace S1API.Internal.Entities
{
    /// <summary>
    /// Identifies the native root component and framework data-object shape used by a custom NPC prefab.
    /// </summary>
    internal enum NpcRootRole
    {
        Plain,
        Dealer,
        Supplier
    }
}
